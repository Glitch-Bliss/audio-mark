import { Meteor } from 'meteor/meteor';
import '../imports/api/audiomarks/methods.js';
import '../imports/api/audiomarks/server/publications.js';
Meteor.startup(() => {
  // code to run on server at startup
});
